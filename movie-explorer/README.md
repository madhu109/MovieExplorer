# Movie Explorer

React + Vite + Material UI + TMDB Movie Explorer.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Add your TMDB key in `.env`:

```env
VITE_TMDB_API_KEY=YOUR_KEY
```
